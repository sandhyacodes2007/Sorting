#include<bits/stdc++.h>
using namespace std;
void Bubble_Sort(int arr[],int n){
    //for outer loop.
    for(int i=n-1; i>=0; i--){
        
            if(arr[j]>arr[j+1]){
                swap(arr[j], arr[j+1]);

            }
        }
    }
}